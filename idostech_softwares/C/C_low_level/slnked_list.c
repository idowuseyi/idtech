#include "lists.h"
/**
*
* This project is used to learn linked list as simple as it is.
*
* Anyone can follow along at any point in time.
*
*/

struct node* NewNode:
{
NewNode = malloc(sizeof(stuct node));
NewNode->data = 40;
NewNode->next = start;
start = NewNode;
}

printf("%d", NewNode->data);

return (0);
