#include <stdio.h>
#include <stdlib.h>
int main(void)
{
/**    int n, lower_r, upper_r;
    printf("Enter a number:\n");
    scanf("%d",&n);
    lower_r = (n / 10) * 10; /*the arithmetic iperator works from left to write 
   upper_r = lower_r + 10;
  printf("Range of your input value %d is: %d - %d\n\n", n, lower_r, upper_r);
*/

int a, b;
a = 2;
printf("the answer is: %d\n\n", a++);
a = a++;
b = a + 1;
printf("new value %d\n", b);
    return (0);
/**
1.
2.
3.
4.
/**
WAP to merge three number. E.g. a= 1, b= 2, c = 8 is 128.
WAP to print the reverse of a 3 digit number.
Compute the gross salary of Mr. HARISH. Input his basic salary. His DA is
40% of basic salary, and HRA is 20% of basic salary.*/
}